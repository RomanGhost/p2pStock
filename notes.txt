08.01.2024 - сделал соединение между двумя таблицами с соединением многие к одному(много сделок для одного состояния)
сделано с помощью таблицы state, можно сделать состояния через enum, но тогда они будут сразу отражаться в базу данных,
над этим еще предстоит подумать, но скорее буду использовать отдельную таблицу для сохранения значения статуса заявки.
Внутри проекта будет отдельный класс с перечеслением данных статусов из БД(из этих классов будет записываться значения в БД)

09.01.2024 - В проект добавил модели для базы данных(не проверял как они импортируются в контейнер), так же создал
репозитории для данных моделей, нужно будет проверить правильность миграции и добавить схему моделей

28.01.2024 - Добавлен CRUD для Deal, нужно будет после заполнения данных раскомментить, чтобы создалась связь