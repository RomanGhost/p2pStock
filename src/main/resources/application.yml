spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/peer_to_peer
    username: admin
    password: password_12345
    driver-class-name: org.postgresql.Driver
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create

application:
  info:
    api: "/api/v1"
